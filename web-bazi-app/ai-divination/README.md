# AI 算命 / 程序化解读（后端与算法）

本目录预留用于：

- 八字解读、格局、喜忌等**程序化**规则或模型调用；
- 若接入大模型 API，建议在此封装服务层，由 Flask 提供 `POST /api/...` 接口，前端 `ai-fortune/ai-fortune.js` 再请求。

与 `frontend/ai-fortune/` 分工：前者偏**业务与算法**，后者偏**页面交互**。
